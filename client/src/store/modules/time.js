const state = {};

const mutations = {};

const actions = {};

const getters = {
    currentTimeISO: () => new Date().toISOString(),
};

export default {
    namespaced: true,
    state,
    mutations,
    actions,
    getters,
};
